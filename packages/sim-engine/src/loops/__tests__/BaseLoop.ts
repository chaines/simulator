describe('BaseLoop', () => {
  /**
   * Honestly, I have no idea how to unit test this. It's super interconnected,
   * and doesn't actually *do* a whole lot by itself. I could theoretically mock out everything else,
   * but at that point the only thing left to test is the return values match the expected, which
   * typescript already handles?
   */
});
